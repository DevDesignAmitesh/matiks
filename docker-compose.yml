services:
  db:
    image: postgres:17-alpine
    
    ports:
      - 5432:5432
      
    environment:
      - POSTGRES_DB=matiks
      - POSTGRES_PASSWORD=secretpassword
      - POSTRES_USER=postgres